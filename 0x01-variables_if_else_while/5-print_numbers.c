#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * print all single numbers of base 10
 *
 * Return: Always (0)(Success)
 */
int main(void)
{
	int x;
	for (x = 0 ; x < 10 ; x++)
	printf("%d\n", x);
	return (0);
}
